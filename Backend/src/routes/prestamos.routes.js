const { Router } = require('express');
const router = Router();
const prestamosController = require('../controllers/prestamos.controller');

// Rutas base del módulo préstamos

module.exports = router;
